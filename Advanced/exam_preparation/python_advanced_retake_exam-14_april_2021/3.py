def flights(*args):
    proba = {None: 0}
    n = 0
    while args[n] != "Finish":
        proba.items()
        value = args[n+1]
        n += 2


print(flights('Vienna', 256, 'Vienna', 26, 'Morocco', 98, 'Paris', 115, 'Finish', 'Paris', 15))
